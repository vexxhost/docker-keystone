name: build

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main
      - stable/**

jobs:
  image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      packages: write
    steps:
      - uses: vexxhost/docker-atmosphere/.github/actions/checkout@stable/zed
        with:
          repository: openstack/keystone
          ref: 2b77d93b167a93c3899c6abb912d7a6bb65f1f16

      - uses: vexxhost/docker-atmosphere/.github/actions/build-image@stable/zed
        with:
          image-name: keystone
          base-image-tag: zed
          build-contexts: keystone=openstack/keystone
          push: ${{ github.event_name != 'pull_request' }}
